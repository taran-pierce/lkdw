import { MenuStateProvider } from "@/utils/useMenu";

export default function SignIn() {
  return (
    <MenuStateProvider>
      <main>
        <h1>Sign In</h1>
      </main>
    </MenuStateProvider>
  )
}
