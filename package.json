{
  "name": "LKDW",
  "version": "0.3.5",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "cd frontend && next dev",
    "build": "cd frontend && npm install && npx next build",
    "postinstall": "tsc",
    "start": "cd frontend && next start",
    "lint": "cd frontend && next lint"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  }
}
