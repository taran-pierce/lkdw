{
  "name": "LKDW",
  "version": "0.8.6",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "dev": "cd frontend && next dev",
    "build": "cd backend && npm i && npx keystone build",
    "start": "cd frontend && npx next start",
    "lint": "cd frontend && next lint"
  }
}
