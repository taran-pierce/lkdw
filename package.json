{
  "name": "LKDW",
  "version": "0.5.3",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "dev": "cd frontend && next dev",
    "build": "cd frontend && npm i && npx next build",
    "start": "cd frontend && npx next start",
    "lint": "cd frontend && next lint"
  }
}
