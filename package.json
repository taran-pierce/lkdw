{
  "name": "LKDW",
  "version": "0.5.1",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "cd frontend && next dev",
    "build": "cd frontend && npx next build",
    "postinstall": "cd frontend && npm install && cd ../backend && npm i",
    "start": "cd frontend && npx next start",
    "lint": "cd frontend && next lint"
  },
  "dependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18"
  }
}
